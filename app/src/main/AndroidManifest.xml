<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uri.iungointerface">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.login.LoginActivity" />
        <activity android:name=".activities.plans.PlansActivity" />
        <activity android:name=".activities.plans.MyPlansActivity" />
        <activity android:name=".activities.plans.PlanActivity" />
        <activity android:name=".activities.profile.ProfileActivity" />
        <activity android:name=".activities.profile.MyProfileActivity" />
        <activity android:name=".activities.profile.ShowFullScreenImage" />
        <activity android:name=".activities.settings.SettingsHeadersActivity" />
        <activity android:name=".activities.settings.SettingsActivity" />
        <activity android:name=".activities.chat.FriendsActivity" />
        <activity android:name=".activities.chat.ChatActivity" />


    </application>

</manifest>